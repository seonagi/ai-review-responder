# Backend Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=5000

# Database (PostgreSQL)
# Get this from Neon.tech or Supabase (free tier)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/ai_review_responder

# JWT Secret (generate a random string)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Google OAuth (Phase 2 - get from https://console.cloud.google.com)
# See SETUP-GOOGLE.md for complete setup instructions
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret-here
GOOGLE_CALLBACK_URL=http://localhost:5000/api/google/callback

# For production, change callback to:
# GOOGLE_CALLBACK_URL=https://your-backend.railway.app/api/google/callback

# OpenAI API (Phase 3)
# Get from: https://platform.openai.com/api-keys
# Free tier includes $5 credit (sufficient for MVP testing)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxx

# OpenAI Model (optional - defaults to gpt-3.5-turbo)
# Options: gpt-3.5-turbo (faster, cheaper) or gpt-4 (higher quality)
OPENAI_MODEL=gpt-3.5-turbo

# Token Encryption Key (for storing Google OAuth tokens securely)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-byte-hex-encryption-key-here
